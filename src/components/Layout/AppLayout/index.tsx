import React, { FC, useEffect, useRef, useState } from 'react';
import { Layout } from 'antd';

const AppLayout: FC = () => {
  return <Layout style={{ height: '100%' }}></Layout>;
};

export default AppLayout;
